<!DOCTYPE html>
<html lang="en">
<head>
        <% include ../partials/head.ejs %>
</head>
<body>
    <!-- top nav -->
    <% include ../partials/nav.ejs %>

    <!-- Cupcakes -->
    <div class="container">
       
        <div class="row">
            <%treats.forEach((cookie, index) => {%>        
                <div class="col s6 m4 l3">
                    <div class="card medium">
                        <div class="card-image">
                            <img src="<%=cookie.image%>">
                            <span class="card-title"><%=cookie.name.toUpperCase()%></span>
                        </div>
                        <div class="card-content">
                            <p><%=cookie.description%></p>
                            <p>Price: $<%=cookie.unitPrice%>0</p>
                        </div>
                        <div class="card-action">
                            <form action="/cookies/<%=cookie.id%>?_method=DELETE" method="POST">
                                <input type="submit" name="delete" id="delete" value="DELETE" class="btn lime">
                            </form>
                            <a href="cookies/<%=cookie.id%>/edit" class="card-link">Edit</a>
                        </div>
                    </div>
                
                </div> 
            <%})%>
        </div>
        
  
       
        
    </div>



    <!-- bottom / Store Info -->

    <% include ../partials/footer.ejs%>
</body>
</html>